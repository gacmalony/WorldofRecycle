<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorldofRecycle"
        tools:targetApi="31">
        <activity
            android:name=".swipe.SwipeView"
            android:exported="false" />
        <activity
            android:name=".multipleselection.MultipleSelection"
            android:exported="false" />
        <activity
            android:name=".MultipleView.MultipleViewActivity"
            android:exported="false" />
        <activity
            android:name=".singleview.SingleActivity"
            android:exported="false" />
        <activity
            android:name=".cardview.CardViewActivity"
            android:exported="false"
            android:label="@string/title_activity_card_view"
            android:theme="@style/Theme.WorldofRecycle" />
        <activity
            android:name=".NormalView.NormalRecycle"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>